@model Int32

@{
    var base64 = Convert.ToBase64String((ViewBag.ImageBytes as byte[]));
    var imgSrc = String.Format("data:image/png;base64,{0}", base64);
}

<h1>XamaMe @Model</h1>
<img src="@imgSrc"/>
